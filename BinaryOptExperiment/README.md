# Binary Family Experiment

`BinaryMain.py` runs the generic-pipeline binary study with at least 100
single-underlying configurations and 100 basket configurations.

Generated CSV files are written to `BinaryOptExperiment/results/` and are
ignored by Git. The committed summary is written to
`Markdown/Binary/results/summary.md`.
