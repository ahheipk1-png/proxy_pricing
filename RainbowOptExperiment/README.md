# Rainbow Family Experiment

`RainbowMain.py` runs the generic-pipeline rainbow study with at least 100
single-underlying configurations and 100 basket configurations.

Generated CSV files are written to `RainbowOptExperiment/results/` and are
ignored by Git. The committed summary is written to
`Markdown/Rainbow/results/summary.md`.
