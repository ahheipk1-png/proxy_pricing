# Yield Seeker Family Experiment

`YieldSeekerMain.py` runs the generic-pipeline yield seeker study with at least
100 single-underlying configurations and 100 basket configurations.

Generated CSV files are written to `YieldSeekerOptExperiment/results/` and are
ignored by Git. The committed summary is written to
`Markdown/YieldSeeker/results/summary.md`.
